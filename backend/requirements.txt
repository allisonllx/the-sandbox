# Runtime
fastapi==0.138.0
uvicorn[standard]==0.49.0
pydantic==2.10.3
openai>=1.30.0          # LLM scoring + Micro-PRD generation (requires OPENAI_API_KEY env var)

# NLP / NER (local, fully offline)
spacy==3.7.0
# After installing spaCy, download the NER model via pip (more reliable than the CLI):
#   pip install "https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl"
# The proxy degrades gracefully (regex-only mode) if the model is absent.

# Dev / Test
pytest==9.1.1
httpx==0.28.1          # async test client for FastAPI endpoints
pytest-asyncio==1.4.0
